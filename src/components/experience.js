import React from "react"
const tempStyles = {
    display: "block",
    minHeight: "500px"
}

export default function Layout() {
 
  return (
    <div id="experience" style={tempStyles}>
        Experience
    </div>
  )
}
